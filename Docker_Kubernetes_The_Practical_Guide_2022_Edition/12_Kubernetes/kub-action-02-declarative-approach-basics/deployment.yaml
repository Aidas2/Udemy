apiVersion: apps/v1
kind: Deployment
metadata:
  name: second-app-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: second-app
      tier: backend
  template:
    metadata: 
      labels:
        app: second-app
        tier: backend
    spec: 
      containers:
        - name: second-node
          # image: aidaspupelis/kub-first-app:2
          # image: aidaspupelis/kub-first-app:3
          image: aidaspupelis/kub-first-app:latest
        # - name: ...
        #   image: ...
